<!-- 원본 안내: 팀 구조(폴더 트리 + 팀/역할 문서)의 원본은 00_Team/ProjectTeam_양식[팀명]/ 이다.
     /new_team은 그 양식 폴더를 복사한다. 이 파일은 양식 폴더 유실 시의 예비 사본이므로 함께 갱신한다. -->

# CLAUDE.md — 팀 관리자: {팀명}

> 생성일: {YYYY-MM-DD} · 이 폴더는 {팀명} 팀의 작업 공간이다.
> 루트 CLAUDE.md의 「전 세션 공통 규칙」이 이 폴더에도 적용된다.

## 팀 목표
(팀이 달성하려는 것 — 생성 후 반드시 작성)

## 팀 폴더 구조

### 필수 폴더 (모든 팀 공통 — 삭제·개명 금지)

| 폴더 | 성격 | 용도 |
|---|---|---|
| `00_Project/` | 공유 | **진행 중 프로젝트 작업물** — `NN_{프로젝트명}/` (프로젝트별 git 저장소 연결 가능) + `process.md` |
| `10_Dashboard/` | 관리 | 팀 현황판 `DASHBOARD.md` (`/report`가 갱신, 루트가 수거) |
| `11_team_doc/` | 공유 | **문서 취합** — 수명 단계 폴더(`00_final` 확정본 / `10_working` 작업 중 / `90_old` 대체본). 보고서(`report_{날짜}.md`)·파이프라인 결과는 폴더 바로 아래(명령이 파싱) |
| `90_result_output/` | 보관 | **완료 후 백업** — 패키지 역할이 최종 선정한 작업물 보관 |
| `CLAUDE.md` | 관리 | 이 문서 (팀 관리자 정의) |

### 역할 작업 공간 (표준 양식)

| 폴더 | 역할 | 임무 |
|---|---|---|
| `01_planner/` | 기획 | **기획·디자인 담당** — 요구사항·기획서·디자인 문서 |
| `02_developer/` | 개발 | **프로그램 개발 과정 담당** — 코드 작업은 `00_Project/`에서 수행 |
| `03_package/` | 패키지 | **배포·최종 선정 담당** — 검증·패키징, `90_result_output/` 쓰기 주체 |

각 역할 폴더: `CLAUDE.md`(역할 정의) + `handover_{역할}.md`(역할 인수인계) + `.claude/settings.json`(공유 폴더 접근 허용).

### 접근 권한 매트릭스 (팀 내 상호 호환)

| 폴더 | planner | developer | package | 팀 관리자 |
|---|---|---|---|---|
| `00_Project/` 작업물 | ✅ | ✅ (코드) | ✅ (검증) | ✅ |
| `00_Project/*/process.md` | 읽기 | 읽기 | 읽기 | ✅ **전담 갱신** |
| `11_team_doc/` | ✅ | ✅ | ✅ | ✅ |
| `90_result_output/` | 읽기 | 읽기 | ✅ **쓰기 주체** | ✅ |
| 자기 역할 폴더 | ✅ | ✅ | ✅ | 읽기 (취합) |
| 타 역할 폴더 / `10_Dashboard` | ❌ | ❌ | ❌ | ✅ (Dashboard는 /report만) |

역할 세션의 공유 폴더 접근은 각 역할 폴더의 `.claude/settings.json`(additionalDirectories)이 허용한다. 미지원 환경에서는 `claude --add-dir` 또는 프롬프트 승인으로 동작 — 위 매트릭스 범위가 규칙이다.

### 커스텀 역할 (표준 양식과 다른 구성이 필요할 때)
- 직접 명령(세션)을 주는 폴더에는 반드시 `CLAUDE.md` + `handover_{역할}.md`를 생성하고, 공유 접근이 필요하면 `.claude/settings.json`을 복사한다.
- 폴더명은 `NN_{역할}` 권장 (예: `04_designer`). 필수 폴더(00/10/11/90)와 이름이 겹치면 안 된다.

## 프로젝트 관리 (00_Project) — 팀 관리자의 핵심 업무

- 생성: 팀 폴더에서 `/new_project {프로젝트명}` → `00_Project/NN_{프로젝트명}/process.md`.
- **`process.md`의 갱신은 팀 관리자 전담**이다. 팀원은 자기 `handover_{역할}.md`에 **`[NN_프로젝트명]` 태그**로 기록하고, 팀 관리자가 `/handover`(팀 폴더)로 취합하면서 각 process.md에 반영한다.
- 수명주기 관리(팀 관리자 전담): 단계 전환(기획→개발→패키지) · 우선순위 · 목표일 · `[보류]`/`[종료]` 표기 — 변경 시 진행 기록에 `[팀 관리자]` 한 줄을 남긴다.
- 프로젝트 폴더는 독립 git 저장소일 수 있다 — 템플릿 저장소는 `process.md`만 추적한다 (루트 .gitignore).
- 종료: 완료 조건 충족 확인 → 패키지 역할이 `90_result_output/`에 백업 → process.md 제목에 `[종료]` → 아카이브 후보.

## 작업 흐름

기획(`01_planner`) → 개발(`02_developer`) → 패키지(`03_package`).
팀원은 역할 폴더에서 세션을 열어 공유 폴더(`00_Project`/`11_team_doc`/`90_result_output`)로 작업하고, 기록은 자기 handover에 태그로 남긴다.

## 이 폴더 세션의 역할 (팀 관리자)
- **`/handover` (핵심 루틴)**: 역할 handover 3개를 읽고 → ① 태그 기반으로 각 `process.md` 갱신 ② 팀 `handover.md` 취합.
- **Builder(구현) 겸임**: 구현이 필요하면 `00_Project/{프로젝트}/`에서 직접 수행.
- 주요 명령: `/new_project` / `/handover` / `/report`(보고 + 팀 대시보드 갱신) / `/pipeline`(분석 → `11_team_doc/`).
- 역할 폴더의 작업 파일은 직접 수정하지 않는다 — 해당 역할 세션에 위임.
- 상위 폴더(루트·타 팀)의 파일에 접근하지 않는다. 전사 현황이 필요하면 사용자에게 요청한다.

## 팀 고유 규칙
- 팀명·폴더·파일명은 영문만 사용한다 (한글은 특이 경우에 한해 최대한 짧게).
- (코딩 컨벤션, 리뷰 규칙 등 팀에서 추가)
